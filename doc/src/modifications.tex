\section{Modification history}

\subsection{PS1-1 - Mainboard}

\subsubsection{Modifications PS1-1AA $\to$ AB}

\begin{ModTable}

\ModItemDone
{BMS wakeup from shutdown}
{When the \ac{BMS} enters shutdown mode, it can only be brought back to normal mode by a rising edge on the TS or VC0 pin. The shutdown mode is entered when sending the SHUTDOWN() command twice or when a low battery voltage is detected. After deep-discharging the battery it is not possible to charge the batteries because the \ac{BMS} remains in shutdown mode. }
{The TS pin should therefore be connected to a GPIO pin (PF0, pin 5) of the microcontroller with a wire to allow wakeup from shutdown mode. }
{daniw}
{2025-07-02}

\ModItemDone
{BMS wakeup from shutdown}
{When the \ac{BMS} enters shutdown mode, it can only be brought back to normal mode by a rising edge on the TS or VC0 pin. The shutdown mode is entered when sending the SHUTDOWN() command twice or when a low battery voltage is detected. After deep-discharging the battery it is not possible to charge the batteries because the \ac{BMS} remains in shutdown mode. }
{The TS pin should be connected to a GPIO pin (PF0, pin 5) of the microcontroller with a wire to allow wakeup from shutdown mode. }
{daniw}
{2025-07-02}

\ModItemDone
{Relay footprint mirrored}
{The footprint of the relais is mirrored. }
{The pins of the relay must be bent and extended to fit the footprint. This correction increases the height of the PCB stack by about \qty{2}{\milli\meter}, due to which the enclosure cannot be completely closed. }
{daniw}
{2025-07-02}

\ModItemDone
{Undefined signal CONF\_ISO when microcontroller not programmed or in reset}
{The signal CONF\_ISO has no defined state when it is not driven. This can happen, when the microcontroller is not programmed or held in reset. This can lead to the relays K2 and K3 to flatter when the device is powered from the output plugs. }
{A pull-down resistor to GND should be manually placed between gate and source of Q13. }
{daniw}
{2025-07-02}

\ModItemDone
{Voltage drop on UI supply}
{The voltage drop of the supplies on the user interface is too large. }
{R188 and R189 should be replaced with 0R/0603. }
{daniw}
{2025-07-02}

\ModItemDone
{High voltage detection always active when battery powered}
{The high voltage detection is always active when the device is battery-powered. This is independent of the voltage across the output plugs. It is caused by leakage current from the PRIM\_ZERO pin to the V\_OUT signal through the microcontroller. }
{Pin 2 (cathode) of D11 and D14 are lifted and connected to the respective pad using a \qty{100}{\kilo\ohm} resistor. A \qty{2.2}{\volt} Zener diode is placed between pin 2 (cathode) and pin 1 (anode) of each diode D11 and D14. }
{daniw}
{2025-07-02}

\ModItemDone
{Inrush current at startup}
{The inrush current at startup is \qty{26}{\ampere}. This triggers the \ac{BMS}, which disables the device. }
{A temporary workaround is to keep the startup button pushed until the \ac{BMS} resets. The \ac{BMS} switches the \acp{MOSFET} much slower which leads to a lower inrush current of approximately \qty{0.7}{\ampere}. }
{daniw}
{2025-07-02}

\ModItemDone
{Low resolution for battery current measurement}
{The resolution of the battery current measurement is \qty{16}{\milli\ampere} with a range of $\pm$\qty{33}{\ampere}. A current range of $\pm$\qty{10}{\ampere} is sufficient. }
{By increasing R19 and R22 to \qty{330}{\kilo\ohm}, the range is changed to $\pm$\qty{10}{\ampere} with a resolution of \qty{4.88}{\milli\ampere}. }
{daniw}
{2025-07-02}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-1AB $\to$ AC}

\begin{ModTable}

\ModItemDone
{Converter not working}
{The parasitic inductance is too large for proper operation of the flyback converter. To allow a demonstration of the \qty{60}{\volt} output, the converter is modified to a boost topology. }
{The primary windings (PS1-102) the high-voltage winding (PS1-124) are removed from the transformer. The relais K1 to K3 are removed and bridged with the \qty{0}{\ohm} resistors R65 and R78. T\_S2 is connected to VIN with a wire. A diode STPS40150CG-TR is soldered from T\_S0 (anode) to R78 (cathode). }
{daniw}
{2025-09-03}

\ModItemDone
{Zero-crossing detection not needed for demonstration device}
{The zero-crossing detection is not needed for the demonstration device and can therefore be disabled. }
{Remove R69 and R79. }
{daniw}
{2025-09-03}

\ModItemDone
{Charging not possible with boost converter}
{As the boost converter is non-synchronous and therefore unidirectional, the device can not be charged thtough the output terminals. The sense terminals are not used for the demonstration devices and can be adapted for battery charging. }
{Connect MEAS- to GND using a wire. Also connect MEAS+ to VIn through a diode. (anode MEAS+, cathode VIN)}
{daniw}
{2025-09-03}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-1AA $\to$ BA}

\begin{ModTable}

\ModItemDone
{BMS wakeup from shutdown}
{When the \ac{BMS} enters shutdown mode, it can only be brought back to normal mode by a rising edge on the TS or VC0 pin. The shutdown mode is entered when sending the SHUTDOWN() command twice or when a low battery voltage is detected. After deep-discharging the battery it is not possible to charge the batteries because the \ac{BMS} remains in shutdown mode. }
{The TS pin should be connected to a GPIO pin of the microcontroller to allow wakeup from shutdown mode. }
{daniw}
{2025-09-10}

\ModItemDone
{Relay footprint mirrored}
{The footprint of the relais is mirrored. }
{The footprint must be corrected. }
{daniw}
{2025-09-12}

\ModItemDone
{Undefined signal CONF\_ISO when microcontroller not programmed or in reset}
{The signal CONF\_ISO has no defined state when it is not driven. This can happen, when the microcontroller is not programmed or held in reset. This can lead to the relays K2 and K3 to flatter when the device is powered from the output plugs. }
{A pull-down resistor to GND should be placed on the signal CONF\_ISO. }
{daniw}
{2025-09-11}

\ModItemDone
{Voltage drop on UI supply}
{The voltage drop of the supplies on the user interface is too large. }
{R188 and R189 should be replaced with 0R/0603. }
{daniw}
{2025-09-10}

\ModItemDone
{Insufficient memory}
{The flash memory of the microcontroller is barely sufficient. As the \ac{OLED} display is replaced with a color LCD, additional memory is needed. }
{It should be considered to replace the microcontroller with a similar type that includes more flash memory. The STM32G474 series seems to be suitable. An external \ac{QSPI} flash memory should be connected to the microcontroller. }
{daniw}
{2025-12-19}

\ModItemDone
{High voltage detection always active when battery powered}
{The high voltage detection is always active when the device is battery-powered. This is independent of the voltage across the output plugs. It is caused by leakage current from the PRIM\_ZERO pin to the V\_OUT signal through the microcontroller. }
{The clamping circuit must be modified to clamp to a lower voltage than the supply voltage. }
{daniw}
{2025-09-11}

\ModItemDone
{Inrush current at startup}
{The inrush current at startup is \qty{26}{\ampere}. This triggers the \ac{BMS}, which disables the device. }
{A precharge circuit should be added to the on/off controller to limit the inrush current. }
{daniw}
{2025-12-19}

\ModItemDone
{Low resolution for battery current measurement}
{The resolution of the battery current measurement is \qty{16}{\milli\ampere} with a range of $\pm$\qty{33}{\ampere}. A current range of $\pm$\qty{10}{\ampere} is sufficient. }
{By increasing R19 and R22 to \qty{330}{\kilo\ohm}, the range is changed to $\pm$\qty{10}{\ampere} with a resolution of \qty{4.88}{\milli\ampere}. }
{daniw}
{2025-09-11}

\ModItemDone
{Clearance around battery terminals}
{The pull-up resistors for the I2C bus are very close to the battery terminals. This can lead to a short circuit and damage of components during assembly. }
{A clearance of a few millimeters around the battery terminals should be implemented. }
{daniw}
{2025-09-12}

\ModItemOpen
{\acs{UI} connector misplacement}
{During assembly of the lid, the \ac{UI} connector can be misplaced, which can lead to short circuits and damage of components. }
{The \ac{UI} connector should be replaced with a type that includes a mechanical guide. }
{}
{}

\ModItemDone
{Converter topology}
{Due to the excessive stray inductance, the flyback with switchable output range does not work as intended. The stray inductance causes overvoltage on the primary and secondary \ac{MOSFET}}
{The converter topology should be changed. A 4-switch buck-boost should work for the lower voltage ranges. A triple-output flyback similar to \url{https://www.analog.com/media/en/reference-design-documentation/design-notes/DN1047.pdf} could be used for the high voltage range. }
{daniw}
{2025-09-17}

\ModItemDone
{Noise on \acs{ADC} measurements}
{The power supply for the \ac{ADC} contains noise. This is caused by the high peak supply current of the display, as well as the converter. This is especially critical, as the reference voltage for the internal \ac{ADC} is derived from the analog supply voltage. }
{A separate supply voltage for the analog circuitry should be implemented using a linear regulator. }
{daniw}
{2025-12-19}

\ModItemDone
{High voltage detection sensitive to current from GPIO}
{The high voltage detection can easily be influenced by current from the microcontroller \ac{GPIO}. }
{A unity-gain amplifier should be added between the high voltage detection voltage divider and the \ac{GPIO}. }
{daniw}
{2025-10-06}

\ModItemDone
{Unreliable overvoltage latch without reset}
{The latch of the overvoltage protection circuit is unreliable and cannot be reset by the microcontroller. }
{Adjust the upper resistor of the reference voltage divider to \qty{6.8}{\kilo\ohm} and the feedback resistor to \qty{2}{\kilo\ohm}. Add \ac{MOSFET} pulldown to startup reset circuit to allow latch reset by the microcontroller. }
{daniw}
{2025-10-06}

\ModItemDone
{Insufficient fuse rating}
{the voltage rating of the fuse is \qty{500}{\volt}, which is insufficient for connecting it to a tractive system with a voltage of \qty{600}{\volt}. }
{The fuse should be replaced with a type that has a voltage rating of at least \qty{600}{\volt}DC. As the maximum tractive system voltage might be increased in a few years, a rating of \qty{1000}{\volt}DC should be considered. }
{daniw}
{2025-10-07}

\ModItemDone
{Battery terminal slot size}
{The slot for the battery terminals has a length of \qty{6}{\milli\meter} which is too short for the cell tab, which also has a width of \qty{6}{\milli\meter}. }
{The length of the slot for the battery terminals should be increased to \qty{8}{\milli\meter}. }
{daniw}
{2025-10-16}

\ModItemDone
{Incorrect gain of the V\_Sens measurement channel. }
{The gain of the V\_Sens \ac{ADC} channel is too low by a factor of 6. This is caused by the voltage divider being loaded by the input impedance of the \ac{ADC} which is \qty{300}{\kilo\ohm}. }
{An amplifier should be added between the voltage divider and the \ac{ADC}. }
{daniw}
{2025-12-19}

\ModItemDone
{Voltage dependence of isolation current limit}
{The isolation measurement current limit using resistors is proportional to the voltage drop across it. }
{A controlled current limiter should be implemented for the isolation measurement. }
{daniw}
{2025-12-19}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-1BA $\to$ BB}

\begin{ModTable}

\ModItemOpen
{Buck boost inductor clearance}
{The overlapping footprints of the buck boost inductor variants could potentially create a short circuit between the switch nodes and the core of the inductor PQ2012J. }
{It is highly recommended to inspect the insulation between the switch nodes and the core of the inductor and add insulating kapton tape if neccessary. }
{}
{}

\ModItemDone
{Swapped supply pins of U17 and U20}
{The supply pins of the operational amplifiers U17 and U20 are swapped, which leads to them being reverse-polarity powered. }
{The trace between the bypass capacitors (C75 and C84) and the V- pins (2) of the operational amplifiers U17 and U20 must be cut. The now disconnected pins must be connected to the GND side of the bypass capacitors using enameled wire. \newline The supply pins (5) of the operational amplifiers U17 and U20 should be lifted and connected to the +3V3\_A side of the bypass capacitors using enameled wire. }
{daniw}
{2026-05-19}

\ModItemDone
{Insufficient \ac{I2C} level}
{The high level of the \ac{I2C} signals is only \qty{2.4}{\volt} instead of the expected \qty{3.3}{\volt}. \newline This is caused by the reverse voltage of the zener diodes D35 and D36 at low current. }
{The zener diodes D35 and D36 should be removed. \newline \textbf{Caution!} This removes the overvoltage protection from the \ac{I2C} signals. }
{daniw}
{2026-05-19}

\ModItemDone
{Inaccurate voltage reference buffer output}
{The voltage reference buffer output is inaccurate. The buffer requires an output capacity in the range of \qty{0.5}{\micro\farad} $\ldots$ \qty{1.5}{\micro\farad}. }
{A \qty{1}{\micro\farad} capacitor should be added between the input (pin 3) of the external reference voltage buffer (U22) and the GND pin of C146. }
{daniw}
{2026-05-19}

\ModItemDone
{On/off controller unable to turn on}
{While precharging V\_IN, Q12 turns the prescharge off due to the pull-up resistor R37 before the comparator U3 is operating, which stops the precharge from continuing. }
{A \qty{47}{\kilo\ohm} resistor should be added from the gate of Q12 to GND. }
{daniw}
{2026-05-19}

\ModItemDone
{On/off controller does not activate main discharge MOSFET}
{The precharge detection MOSFET Q11 is unable to detect the completion of the precharge due to the still too high voltage at its gate. }
{Change R40 to \qty{1}{\kilo\ohm} or add a \qty{1.1}{\kilo\ohm} resistor parallel to R40. }
{daniw}
{2026-05-19}

\ModItemDone
{Overvoltage protection latch reset}
{The overvoltage protection latch reset does not work due to insufficient voltage at the positive input of the latch comparator. }
{Change R82 to \qty{2.4}{\kilo\ohm}. }
{daniw}
{2026-05-19}

\ModItemDone
{Overcurrent protection always active}
{The overcurrent protection is always active because the threshold does not account for the offset $\frac{VREF}{2}$ on the current mesurement. }
{Change R105 to \qty{1}{\mega\ohm} and R106 to \qty{330}{\kilo\ohm}. }
{daniw}
{2026-05-19}

\ModItemDone
{Offset in isolation current measurement}
{The isolation current measurement has an offset of \qty{129}{\micro\ampere} due to the current introduced by U14A. }
{Remove R135. }
{daniw}
{2026-05-29}

\ModItemOpen
{Ringing in current controller at turn-on}
{When increasing the current from \qty{0}{\ampere} the current shows ringing before reaching the set value. }
{Check if adding \qty{1}{\micro\farad} parallel to R144 removes the ringing. }
{}
{}

\ModItemDone
{Current measurement noise}
{The noise of the current measurement is aproximately \qty{100}{\milli\ampere}, which is caused by the input noise of the instrumentation amplifier. }
{The instrumentation amplifier should be replaced by a current sense amplifier such as INA181. The battery current measurement and isolation current measurement could also be changed to using an INA181. }
{2026-05-22}
{}

\ModItemDone
{Unreliable QSPI operation}
{The level of the pins QSPI.IO2 and QSPI.IO3 is undefined in single and dual SPI mode, which can lead to the flash entering hold or write protect mode. }
{Pull- up resistors should be added to the QSPI.IO2 and QSPI.IO3 pins. It is recommended to add a pull-up resistor fo CS/ as well. }
{daniw}
{2026-05-20}

\ModItemDone
{No detection of open SENS wires}
{The device is unable to detect if the SENS wires are connected to the \ac{DUT}. }
{A pull-up resistor should be added to SENS+ and a pull-down resistor should be added to SENS-. Due to the high voltage that can be present at the terminals, the resistors must likely be implemented using multiple resistors in series. }
{daniw}
{2026-05-27}

\ModItemDone
{Incomplete precharge}
{With the LEDs and the LCD running, the current consumption is too high (\qty{115}{\milli\ampere}) for the precharge to complete. }
{The precharge current should be increased to $\geq$ \qty{180}{\milli\ampere}. This can be achieved by reducing the value of the precharge current resistors R22 \ldots R25 to \qty{15}{\ohm}. This results in a precharge current of ???. }
{daniw}
{2026-05-26}

\ModItemDone
{Display unreliable at maximum speed}
{When the display is run at the maximum clock speed of \qty{40}{\mega\hertz}, the communication is unreliable. }
{Change resistors R206, R208, R210, R211, R212, R213 and R215 to \qty{47}{\ohm} or add \qty{62}{\ohm} in parallel. }
{daniw}
{2026-06-21}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-1BA $\to$ CA}

\begin{ModTable}

\ModItemOpen
{Overvoltage category}
{The clearance is sufficient for \qty{600}{\volt} CAT II. But the formula student rules \cite{FSG:Rules_2026} require \qty{600}{\volt} CAT III for measurement equipment that is used for accumulator inspection. }
{The clearance should be increased to \qty{5.5}{\milli\meter} for basic insulation and \qty{11}{\milli\meter} for reinforced insulation to meet the criteria for \qty{600}{\volt} CAT III according to IEC 61010-1. }
{}
{}

\ModItemOpen
{Buck boost inductor selection}
{The overlapping footprints of the buck boost inductor variants could potentially create a short circuit between the switch nodes and the core of the inductor PQ2012J. }
{After testing, a single inductor type should be selected. }
{}
{}

\ModItemOpen
{Swapped supply pins of U17 and U20}
{The supply pins of the operational amplifiers U17 and U20 are swapped, which leads to them being reverse-polarity powered. }
{The supply pins of U17 and U20 must be swapped. \newline Additionally, renaming the supply pins of all operational amplifiers to V+ and V- is highly recommended for easier recognition of swapped supply pins during schematic review. }
{}
{}

\ModItemOpen
{Insufficient \ac{I2C} level}
{The high level of the \ac{I2C} signals is only \qty{2.4}{\volt} instead of the expected \qty{3.3}{\volt}. \newline This is caused by the reverse voltage of the zener diodes D35 and D36 at low current. }
{The zener diodes D35 and D36 should be replaced with double diodes to clamp the \ac{I2C} signals to the +3V3 supply. }
{}
{}

\ModItemOpen
{Inaccurate voltage reference buffer output}
{The voltage reference buffer output is inaccurate. The buffer requires an output capacity in the range of \qty{0.5}{\micro\farad} $\ldots$ \qty{1.5}{\micro\farad}. }
{A \qty{1}{\micro\farad} capacitor should be added to the signal VREF\_UC. }
{}
{}

\ModItemOpen
{On/off controller unable to turn on}
{While precharging V\_IN, Q12 turns the prescharge off due to the pull-up resistor R37 before the comparator U3 is operating, which stops the precharge from continuing. }
{A \qty{47}{\kilo\ohm} resistor should be added from the gate of Q12 to GND. }
{}
{}

\ModItemOpen
{On/off controller does not activate main discharge MOSFET}
{The precharge detection MOSFET Q11 is unable to detect the completion of the precharge due to the still too high voltage at its gate. }
{Change R40 to \qty{1}{\kilo\ohm} or add a \qty{1.1}{\kilo\ohm} resistor parallel to R40. }
{}
{}

\ModItemOpen
{Overvoltage protection latch reset}
{The overvoltage protection latch reset does not work due to insufficient voltage at the positive input of the latch comparator. }
{Change R82 to \qty{2.4}{\kilo\ohm}. }
{}
{}

\ModItemOpen
{Overcurrent protection always active}
{The overcurrent protection is always active because the threshold does not account for the offset $\frac{VREF}{2}$ on the current mesurement. }
{Change R105 to \qty{1}{\mega\ohm} and R106 to \qty{330}{\kilo\ohm}. }
{}
{}

\ModItemOpen
{Offset in isolation current measurement}
{The isolation current measurement has an offset of \qty{129}{\micro\ampere} due to the current introduced by U14A. }
{Remove R135. }
{}
{}

\ModItemOpen
{Ringing in current controller at turn-on}
{When increasing the current from \qty{0}{\ampere} the current shows ringing before reaching the set value. }
{Check if adding \qty{1}{\micro\farad} parallel to R144 removes the ringing. }
{}
{}

\ModItemOpen
{Current measurement noise}
{The noise of the current measurement is aproximately \qty{100}{\milli\ampere}, which is caused by the input noise of the instrumentation amplifier. }
{The instrumentation amplifier should be replaced by a current sense amplifier such as INA181. The battery current measurement and isolation current measurement should also be changed to using an INA181. }
{}
{}

\ModItemOpen
{Unreliable QSPI operation}
{The level of the pins QSPI.IO2 and QSPI.IO3 is undefined in single and dual SPI mode, which can lead to the flash entering hold or write protect mode. }
{Pull- up resistors should be added to the QSPI.IO2 and QSPI.IO3 pins. It is recommended to add a pull-up resistor fo CS/ as well. }
{}
{}

\ModItemOpen
{No detection of open SENS wires}
{The device is unable to detect if the SENS wires are connected to the \ac{DUT}. }
{A pull-up resistor should be added to SENS+ and a pull-down resistor should be added to SENS-. Due to the high voltage that can be present at the terminals, the resistors must likely be implemented using multiple resistors in series. }
{}
{}

\ModItemOpen
{Incomplete precharge}
{With the LEDs and the LCD running, the current consumption is too high (\qty{115}{\milli\ampere}) for the precharge to complete. }
{The precharge current should be increased to $\geq$ \qty{180}{\milli\ampere}. This can be achieved by reducing the value of the precharge current resistors R22 \ldots R25 to \qty{15}{\ohm}. This results in a precharge current of ???. }
{}
{}

\ModItemDone
{Display unreliable at maximum speed}
{When the display is run at the maximum clock speed of \qty{40}{\mega\hertz}, the communication is unreliable. }
{Change resistors R206, R208, R210, R211, R212, R213 and R215 to \qty{47}{\ohm}. }
{daniw}
{2026-06-21}

\end{ModTable}

\FloatBarrier

\subsection{PS1-2 - User interface}

\subsubsection{Modifications PS1-2AA $\to$ AB}

\begin{ModTable}

\ModItemDone
{\acs{I2C} signals swapped}
{The \ac{I2C} signals SCL nad SDA are swapped on the header in comparison to the main PCB PS1-1. }
{The \ac{I2C} signals need to be cross-connected using wires. }
{daniw}
{2025-07-05}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-2AB $\to$ AC}

\begin{ModTable}

\ModItemDone
{Noise on \qty{3.3}{\volt} supply}
{The supply current of the \ac{OLED} display includes large peaks, which lead to excessive noise on the \qty{3.3}{\volt} supply. }
{A \ac{LDO} voltage regulator which is supplied from VCC generates a supply voltage of \qty{3.3}{\volt}, which is supplied to the display. }
{daniw}
{2025-09-04}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-2AA $\to$ BA}

\begin{ModTable}

\ModItemDone
{LED polarity marking}
{The LEDs have no polarity markings. This was caused by the insufficient spacing between the polarity marker and the cathode pad, which resulted in the automatic removal of the marker during silkscreen cleanup. }
{The marker should be restored and moved away from the pad. }
{daniw}
{2025-09-10}

\ModItemDone
{Labels for plugs poorly readable}
{The labels of the plugs are partly located under the step of the enclosure lid and therefore poorly readable. }
{The labels should be moved. }
{daniw}
{2025-09-10}

\ModItemDone
{\acs{I2C} signals swapped}
{The \ac{I2C} signals SCL and SDA are swapped on the header in comparison to the main PCB PS1-1. }
{The \ac{I2C} signals need to be swapped back. }
{daniw}
{2025-09-09}

\ModItemDone
{Display readability in bright sunlight}
{The display is barely readable when the device is used in bright sunlight. }
{To be readable in direct sunlight a brightness of at least \qty{600}{\candela\per\square\meter} is required. A suitable replacement would be the type NHD-2.4-240320AF from Newhaven Display. In a dark environment the display will likely be too bright. It is recommended to use an ambient light sensor to adjust the brightness of the display to the environment. }
{daniw}
{2025-12-19}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-2BA $\to$ BB}

\begin{ModTable}

\ModItemOpen
{Unreliable ambient light sensor}
{The ambient light sensor is partly unresponsive or responds on a differend \ac{I2C} address. }
{}
{}
{}

\ModItemOpen
{Display not working}
{The IM1 and IM2 pins of the display have an internal \qty{10}{\kilo\ohm} pull-down resistor. The external pull-up resistors are \qty{10}{\kilo\ohm}, which is not low enough to set the communication mode of the display to 4-wire \ac{SPI}. }
{The value of the pull-up resistors R30 and R31 should be reduced to \qty{1}{\kilo\ohm}. }
{}
{}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications PS1-2BA $\to$ CA}

\begin{ModTable}

\ModItemOpen
{Overvoltage category}
{The clearance is sufficient for \qty{600}{\volt} CAT II. But the formula student rules \cite{FSG:Rules_2026} require \qty{600}{\volt} CAT III for measurement equipment that is used for accumulator inspection. }
{The clearance should be increased to \qty{5.5}{\milli\meter} for basic insulation and \qty{11}{\milli\meter} for reinforced insulation to meet the criteria for \qty{600}{\volt} CAT III according to IEC 61010-1. }
{}
{}

\ModItemOpen
{Hole size for breakouts}
{The clearance between the holes of the pushbutton breakout and the tracks running between them is too low for manufacturing. }
{The hole size or location must be adjusted. }
{}
{}

\ModItemOpen
{Unreliable ambient light sensor}
{The ambient light sensor is partly unresponsive or responds on a differend \ac{I2C} address. }
{}
{}
{}

\ModItemOpen
{Display not working}
{The IM1 and IM2 pins of the display have an internal \qty{10}{\kilo\ohm} pull-down resistor. The external pull-up resistors are \qty{10}{\kilo\ohm}, which is not low enough to set the communication mode of the display to 4-wire \ac{SPI}. }
{The value of the pull-up resistors R30 and R31 should be reduced to \qty{1}{\kilo\ohm}. }
{}
{}

\ModItemOpen
{Missmatched component size}
{The size of the component 4U7/0805/16V/X7R does not mathc the footprint. The cause for this is an incorrect supplier link. }
{The supplier link 3 of the component 4U7/0805/16V/X7R should be replaced with a component in 0805 size. }
{}
{}

\end{ModTable}

\FloatBarrier

\subsection{PS1-3 - Programming adapter}

\subsubsection{Modifications on PS1-3 AA $\to$ AB}

\begin{ModTable}

\ModItemOpen
{TAG-Connect assembly side}
{The TAG-Connect header is labelled as being assembled on the bottom side. }
{The header should be assembled on the top side. }
{}
{}

\ModItemOpen
{Insufficient spacing between TAG-Connect header and BOOT header}
{The spacing between TAG-Connect header and BOOT header is insufficient. }
{A 2-pin header should be used for the BOOT header. The header is placed on +3V3 and BOOT pins. This should be sufficient, as there is a pull-down resistor on the BOOT signal on the mainboard. }
{}
{}

\end{ModTable}

\FloatBarrier

\subsubsection{Modifications on PS1-3 AA $\to$ BA}

\begin{ModTable}

\ModItemOpen
{Incorrect connector type for STLINK-V3-MINIE}
{The connector for the STLINK-V3-MINIE has a pitch of \qty{2.54}{\milli\meter}. The spacing should be \qty{1.27}{\milli\meter}. }
{The connector need to be moved. }
{}
{}

\ModItemOpen
{Insufficient spacing between TAG-Connect header and BOOT header}
{The spacing between TAG-Connect header and BOOT header is insufficient. }
{The BOOT header should be moved. }
{}
{}

\end{ModTable}

\FloatBarrier

